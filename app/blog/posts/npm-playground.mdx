export const title = "NPM Playground je tu!";
export const img = "npm-playground.png";
export const date = "2023-06-23";
export const description = "NPM Playground je nový nástroj pro vývojáře, kteří chtějí rychle vyzkoušet kód online. Podporuje NPM balíčky, více souborů a mnoho dalšího.";

Na internetu je mnoha <q>code playgrounds</q>, ale minimum z nich podporuje NPM balíčky. A pokud, tak jsou buďto placené, nebo obrovské. Proto jsem se rozhodl vytvořit NPM Playground.

<a className="px-btn px-btn-theme" href="https://npm-playground.web.app">Spustit NPM Playground</a>

## Podpora NPM balíčků
Lze importovat NPM balíčky pomocí `import`, ale ne `require`.

```js
import $ from "jquery";

$("body").css("background-color", "#FF0");
```

## Podpora více souborů

NPM playground podporu více souborů, které mohou být propojeny pomocí ECMAScript `import` / `export`. Ale pozor - musíte tyto koncovky vyloučit:
* .js
* .jsx
* .ts
* .tsx

To znamená, že pokud máte soubor `counter.tsx`, musíte ho importovat jako `"./counter"`:

index.tsx
```tsx
import Counter from "./counter";

/// ....
```

counter.jsx
```jsx
export default function Counter({count}) {
    return <h1>Counter: {count}</h1>
}
```

## Archivace projektu

Projekt byl ve prospěch [PlayGist](playgist.mdx) archivován. To znamená, že nebude dostávat updaty, ale stále bude dostupný na [GitHubu](https://github.com/tomas-wrobel/npm-playgound).